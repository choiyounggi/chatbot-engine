# 이 파일은 Rasa 서버의 인증 정보를 구성합니다

# REST 채널 설정 (Spring Boot에서 접근할 API 엔드포인트)
rest:
  # 접근을 허용할 URL (기본값: localhost)
  # 프로덕션 환경에서는 적절한 CORS 설정을 권장합니다
  cors_origins:
    - "*"

# 다른 채널 설정은 필요에 따라 추가할 수 있습니다
# socket_io:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true
